DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=church_management

# Web app server (web_server.py)
WEB_HOST=0.0.0.0
WEB_PORT=8080

# Read-only API server (api_server.py) - still used if you run it standalone
API_HOST=127.0.0.1
CORS_ORIGINS=http://127.0.0.1:*,http://localhost:*,http://yourdomain.com
