# API router package: each module in this directory exposes an APIRouter for a
# functional area (auth, members, finance, content, receipts, stats, admin,
# church, sync) and is mounted by the FastAPI app.
