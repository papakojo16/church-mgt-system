"""Backend package for the Mt. Olivet Methodist Church web app.

Exposes the FastAPI application (main.py), its database layer (database.py),
and the core business-logic modules (auth, finance, events, ministries, etc.).
Individual routers under routers/ depend on the helper functions here.
"""
